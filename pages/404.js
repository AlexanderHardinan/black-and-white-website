export default function NotFound() {
  return (
    <main style={{padding:40}}>
      <h1>404 — Page Not Found</h1>
      <p>But you reached the app. Try the <a href="/">homepage</a>.</p>
    </main>
  );
}
